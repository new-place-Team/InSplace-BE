config:
  target: 'https://www.inspro.shop'
  phases:
    - duration: 60
      arrivalRate: 70
scenarios:
  - name: '쿠버 적용 후 60초동안 초당 50명이 메인페이지'
    flow:
      # 메인페이지 조회
      - get:
          url: '/main'
